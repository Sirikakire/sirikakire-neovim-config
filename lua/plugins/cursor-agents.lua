return {
  -- "xTacobaco/cursor-agent.nvim",
  -- keys = require("keymap").cursor_agent_keymaps,
  -- opts = {
  --   -- Executable or argv table. Example: "cursor-agent" or {"/usr/local/bin/cursor-agent"}
  --   cmd = "cursor-agent",
  --   -- Additional arguments always passed to the CLI
  --   args = {},
  -- },
}
